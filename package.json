{
  "name": "jehoz.github.io",
  "scripts": {
    "clean": "shx rm dist/*.js",
    "build": "spago build",
    "serve": "spago build && esbuild --bundle index.js --outdir=dist --servedir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.19.3",
    "shx": "^0.3.4"
  }
}
